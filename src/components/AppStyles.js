import { makeStyles } from '@material-ui/core/styles'

export const useGridStyles = makeStyles(() => ({
    root: {
        marginTop: '10px',
    },
    replaceButton: {
        marginTop: '20px',
    },
}))